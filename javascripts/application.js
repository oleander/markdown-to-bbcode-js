var convert;
convert = function() {
  var data, from, list, to;
  from = $("#from");
  to = $("#to");
  data = from.html();
  return list = function(ingoing) {
    var items;
    items = ingoing.match(/- (\w+)/g);
    return list = "";
  };
};